apiVersion: apps/v1
kind: Deployment
metadata:
  name: pos-ui
  namespace: retail-pos
spec:
  replicas: 3  # Black Friday surge: Scaled up for increased store traffic
  selector:
    matchLabels:
      app: pos-ui
  template:
    metadata:
      labels:
        app: pos-ui
    spec:
      imagePullSecrets:
      - name: acr-secret
      containers:
      - name: pos-ui
        image: retailstorefrontacr.azurecr.io/react-pos:v4.4  # Updated ZAVA logo + winter styling
        ports:
        - containerPort: 80
        env:
        - name: BLACK_FRIDAY_ENABLED
          valueFrom:
            configMapKeyRef:
              name: black-friday-pos-config
              key: BLACK_FRIDAY_ENABLED
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "250m"
        env:
        - name: REACT_APP_PRICING_API_URL
          valueFrom:
            configMapKeyRef:
              name: pricing-config
              key: api_url
              optional: true