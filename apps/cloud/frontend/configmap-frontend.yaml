apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  namespace: apparel-store
data:
  cache-duration: "300"       # 5 minutes cache for normal traffic
  api-timeout: "3000"         # 3 second timeout for backend calls
